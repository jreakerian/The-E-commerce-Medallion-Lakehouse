version: 2

models:
  - name: dim_products
    description: "Dimension table for products with sales and review metrics."
    config:
      contract:
        enabled: true
    tests:
      # -- Volume: detect unexpected row count changes --
      - elementary.volume_anomalies

      # -- Dimension: detect distribution shifts in product categories --
      - elementary.dimension_anomalies:
          dimensions:
            - product_category_name_english

    columns:
      - name: product_id
        data_type: varchar
        description: "Unique identifier for a product."
        tests:
          - unique
          - not_null

      - name: product_category_name
        data_type: varchar
        description: "Product category name (Portuguese)."

      - name: product_category_name_english
        data_type: varchar
        description: "Product category name in English."

      - name: product_name_length
        data_type: number
        description: "Length of product name in characters."
        tests:
          - dbt_expectations.expect_column_values_to_be_between:
              arguments:
                min_value: 0

      - name: product_description_length
        data_type: number
        description: "Length of product description in characters."
        tests:
          - dbt_expectations.expect_column_values_to_be_between:
              arguments:
                min_value: 0

      - name: product_photos_qty
        data_type: number
        description: "Number of product photos."
        tests:
          - dbt_expectations.expect_column_values_to_be_between:
              arguments:
                min_value: 0

      - name: product_weight_g
        data_type: number
        description: "Product weight in grams."
        tests:
          - dbt_expectations.expect_column_values_to_be_between:
              arguments:
                min_value: 0

      - name: product_length_cm
        data_type: number
        description: "Product length in centimeters."
        tests:
          - dbt_expectations.expect_column_values_to_be_between:
              arguments:
                min_value: 0

      - name: product_height_cm
        data_type: number
        description: "Product height in centimeters."
        tests:
          - dbt_expectations.expect_column_values_to_be_between:
              arguments:
                min_value: 0

      - name: product_width_cm
        data_type: number
        description: "Product width in centimeters."
        tests:
          - dbt_expectations.expect_column_values_to_be_between:
              arguments:
                min_value: 0

      - name: total_units_sold
        data_type: number
        description: "Total units of this product sold."
        tests:
          - dbt_expectations.expect_column_values_to_be_between:
              arguments:
                min_value: 0

      - name: total_revenue_generated
        data_type: number(10,2)
        description: "Total revenue generated by this product."
        tests:
          - dbt_expectations.expect_column_values_to_be_between:
              arguments:
                min_value: 0
          - elementary.column_anomalies:
              arguments:
                column_anomalies:
                  - average
                  - max

      - name: average_review_score
        data_type: number
        description: "Average review score for this product (1-5)."
        tests:
          - dbt_expectations.expect_column_values_to_be_between:
              arguments:
                min_value: 1
                max_value: 5
          - elementary.column_anomalies:
              arguments:
                column_anomalies:
                  - average

      - name: total_reviews
        data_type: number
        description: "Total number of reviews for this product."
        tests:
          - dbt_expectations.expect_column_values_to_be_between:
              arguments:
                min_value: 0
